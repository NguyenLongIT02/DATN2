import AppsContainer from "@crema/components/AppsContainer";
import AutomationContent from "./AutomationContent";

const Automation = () => {
  return (
    <AppsContainer title="Automation">
      <AutomationContent />
    </AppsContainer>
  );
};

export default Automation;
